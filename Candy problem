Each child gets at least 1 candy. A child with a higher rating than an adjacent child gets more candies than that neighbor.
Greedy question because each child’s candy count depends only on its neighbors, not the whole array.

If ratings[i] > ratings[i-1] then candies[i] > candies[i-1]. If ratings[i] > ratings[i+1] then candies[i] > candies[i+1].

class Solution {
    public int candy(int[] ratings) {
        int n = ratings.length;
        int[] cd = new int[n];
        Arrays.fill(cd, 1);
        for (int i = 1; i < n; i++) {
            if (ratings[i] > ratings[i - 1]) {
                cd[i] = cd[i - 1] + 1;
            }
        }
        for (int i = n - 2; i >= 0; i--) {
            if (ratings[i] > ratings[i + 1]) {
                cd[i] = Math.max(cd[i], cd[i + 1] + 1);
        }}
        int tot = 0;
        for (int c : cd) tot += c;
        return tot;
    }
}
